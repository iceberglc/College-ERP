# Web framework. Django 5.2 LTS matches the Python 3.11 production runtime.
Django>=5.2,<6.0

# WSGI server (production)
gunicorn==20.0.4

# Static file serving
whitenoise==6.12.0

# PostgreSQL driver (Digital Ocean managed databases)
psycopg2-binary>=2.9.3

# Database URL parsing (reads DATABASE_URL env var)
dj-database-url==0.5.0

# Image handling (profile pictures)
Pillow>=9.0.0

# HTTP requests (Firebase notifications)
requests>=2.27.0

# Environment variable loading from .env
python-dotenv>=0.19.0

# Login brute-force lockout. django-axes 8.x is required for Django 5.x.
django-axes>=8.0

# Mobile API (STEP 3)
djangorestframework>=3.14.0
djangorestframework-simplejwt>=5.3.1
django-cors-headers>=4.3.1

# Persistent media file storage on DigitalOcean Spaces (S3-compatible)
django-storages[s3]>=1.14
boto3>=1.34
